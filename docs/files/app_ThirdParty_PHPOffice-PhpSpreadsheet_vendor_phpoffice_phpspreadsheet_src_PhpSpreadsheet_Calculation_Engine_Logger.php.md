# app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Calculation\Engine\Logger.php

- Path: `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Calculation\Engine\Logger.php`
- Type: PHP
- Size: 3435 bytes

## Summary (from docblocks)

Flag to determine whether a debug log should be generated by the calculation engine
       If true, then a debug log will be generated
       If false, then a debug log will not be generated.
@var bool

Flag to determine whether a debug log should be echoed by the calculation engine
       If true, then a debug log will be echoed
       If false, then a debug log will not be echoed
A debug log can only be echoed if it is generated.
@var bool

The debug log generated by the calculation engine.
@var string[]

The calculation engine cell reference stack.
@var CyclicReferenceStack

Instantiate a Calculation engine logger.

Enable/Disable Calculation engine logging.
@param bool $writeDebugLog

Return whether calculation engine logging is enabled or disabled.
@return bool

Enable/Disable echoing of debug log information.
@param bool $echoDebugLog

Return whether echoing of debug log information is enabled or disabled.
@return bool

Write an entry to the calculation engine debug log.
@param mixed $args

Write a series of entries to the calculation engine debug log.
@param string[] $args

Clear the calculation engine debug log.

Return the calculation engine debug log.
@return string[]

## Symbols

# Symbols

**Files documented**: 1

## `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Calculation\Engine\Logger.php`

**Classes**:
- `PhpOffice\PhpSpreadsheet\Calculation\Engine\Logger`

**Functions/Methods**:
- `__construct(CyclicReferenceStack $stack)`
- `setWriteDebugLog($writeDebugLog)`
- `getWriteDebugLog()`
- `setEchoDebugLog($echoDebugLog)`
- `getEchoDebugLog()`
- `writeDebugLog(string $message, ...$args)`
- `mergeDebugLog(array $args)`
- `clearLog()`
- `getLog()`

