# app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Calculation\Calculation.php

- Path: `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Calculation\Calculation.php`
- Type: PHP
- Size: 233138 bytes

## Summary (from docblocks)

Constants

Regular Expressions

constants

Instance of this class.
@var Calculation

Instance of the spreadsheet this Calculation Engine is using.
@var Spreadsheet

Calculation cache.
@var array

Calculation cache enabled.
@var bool

@var BranchPruner

@var bool

List of operators that can be used within formulae
The true/false value indicates whether it is a binary operator or a unary operator.
@var array

List of binary operators (those that expect two operands).
@var array

The debug log generated by the calculation engine.
@var Logger

Flag to determine how formula errors should be handled
       If true, then a user error will be triggered
       If false, then an exception will be thrown.
@var bool

Error message for any error that was raised/thrown by the calculation engine.
@var null|string

Reference Helper.
@var ReferenceHelper

An array of the nested cell references accessed by the calculation engine, used for the debug log.
@var CyclicReferenceStack

Current iteration counter for cyclic formulae
If the value is 0 (or less) then cyclic formulae will throw an exception,
otherwise they will iterate to the limit defined here before returning a result.
@var int

Number of iterations for cyclic formulae.
@var int

The current locale setting.
@var string

List of available locale settings
Note that this is read for the locale subdirectory only when requested.
@var string[]

Locale-specific argument separator for function arguments.
@var string

Locale-specific translations for Excel constants (True, False and Null).
@var array<string, string>

Excel constant string translations to their PHP equivalents
Constant conversion from text name/value to actual (datatyped) value.
@var array<string, mixed>

Get an instance of this class.
@param ?Spreadsheet $spreadsheet Injected spreadsheet for working with a PhpSpreadsheet Spreadsheet object,
                                   or NULL to create a standalone calculation engine

Flush the calculation cache for any existing instance of this class
       but only if a Calculation instance exists.

Get the Logger for this calculation engine instance.
@return Logger

__clone implementation. Cloning should not be allowed in a Singleton!

Return the locale-specific translation of TRUE.
@return string locale-specific translation of TRUE

Return the locale-specific translation of FALSE.
@return string locale-specific translation of FALSE

Set the Array Return Type (Array or Value of first element in the array).
@param string $returnType Array return type
@return bool Success or failure

Return the Array Return Type (Array or Value of first element in the array).
@return string $returnType Array return type

Is calculation caching enabled?
@return bool

Enable/disable calculation cache.
@param bool $calculationCacheEnabled

Enable calculation cache.

Disable calculation cache.

Clear calculation cache.

Clear calculation cache for a specified worksheet.
@param string $worksheetName

Rename calculation cache for a specified worksheet.
@param string $fromWorksheetName
@param string $toWorksheetName

Enable/disable calculation cache.
@param mixed $enabled

Get the currently defined locale code.
@return string

Set the locale code.
@param string $locale The locale to use for formula translation, eg: 'en_us'
@return bool

Wrap string values in quotes.
@param mixed $value
@return mixed

Remove quotes used as a wrapper to identify string values.
@param mixed $value
@return mixed

Calculate cell value (using formula from a cell ID)
Retained for backward compatibility.
@param Cell $cell Cell to calculate
@return mixed

Calculate the value of a cell formula.
@param Cell $cell Cell to calculate
@param bool $resetLog Flag indicating whether the debug log should be reset or not
@return mixed

Validate and parse a formula string.
@param string $formula Formula to parse
@return array|bool

Calculate the value of a formula.
@param string $formula Formula to parse
@param string $cellID Address of the cell to calculate
@param Cell $cell Cell to calculate
@return mixed

@param mixed $cellValue

@param string $cellReference
@param mixed $cellValue

Parse a cell formula and calculate its value.
@param string $formula The formula to parse and calculate
@param string $cellID The ID (e.g. A3) of the cell that we are calculating
@param Cell $cell Cell to calculate
@return mixed

Ensure that paired matrix operands are both matrices and of the same size.
@param mixed $operand1 First matrix operand
@param mixed $operand2 Second matrix operand
@param int $resize Flag indicating whether the matrices should be resized to match
                                       and (if so), whether the smaller dimension should grow or the
                                       larger should shrink.
                                           0 = no resize
                                           1 = shrink to fit
                                           2 = extend to fit
@return array

Read the dimensions of a matrix, and re-index it with straight numeric keys starting from row 0, column 0.
@param array $matrix matrix operand
@return int[] An array comprising the number of rows, and number of columns

Ensure that paired matrix operands are both matrices of the same size.
@param mixed $matrix1 First matrix operand
@param mixed $matrix2 Second matrix operand
@param int $matrix1Rows Row size of first matrix operand
@param int $matrix1Columns Column size of first matrix operand
@param int $matrix2Rows Row size of second matrix operand
@param int $matrix2Columns Column size of second matrix operand

Ensure that paired matrix operands are both matrices of the same size.
@param mixed $matrix1 First matrix operand
@param mixed $matrix2 Second matrix operand
@param int $matrix1Rows Row size of first matrix operand
@param int $matrix1Columns Column size of first matrix operand
@param int $matrix2Rows Row size of second matrix operand
@param int $matrix2Columns Column size of second matrix operand

Format details of an operand for display in the log (based on operand type).
@param mixed $value First matrix operand
@return mixed

Format type and details of an operand for display in the log (based on operand type).
@param mixed $value First matrix operand
@return null|string

@param string $formula
@return false|string False indicates an error

@param string $formula
@return array<int, mixed>|false

@var array $d

@var int $valx

@param mixed $tokens
@param null|string $cellID
@return array<int, mixed>|false

@param mixed $operand1
@param mixed $operand2
@param string $operation
@return array

@param mixed $operand1
@param mixed $operand2
@param string $operation
@param bool $recursingArrays
@return mixed

@param mixed $operand1
@param mixed $operand2
@param mixed $operation
@param string $matrixFunction
@param mixed $stack
@return bool|mixed

Trigger an error, but nicely, if need be.
@return false

Extract range values.
@param string $range String based range representation
@param Worksheet $worksheet Worksheet
@param bool $resetLog Flag indicating whether calculation log should be reset or not
@return mixed Array of values in range if range contains more than one element. Otherwise, a single value is returned.

Extract range values.
@param string $range String based range representation
@param null|Worksheet $worksheet Worksheet
@param bool $resetLog Flag indicating whether calculation log should be reset or not
@return mixed Array of values in range if range contains more than one element. Otherwise, a single value is returned.

Is a specific function implemented?
@param string $function Function Name
@return bool

Get a list of all implemented functions as an array of function objects.

Get a list of implemented Excel function names.
@return array

@return null|mixed

Add cell reference if needed while making sure that it is the last argument.
@param bool $passCellReference
@param array|string $functionCall
@return array

@return mixed|string

## References

**Database Tables (inferred)**
- `text`
- `the`
- `a`
- `cache`
- `row`
- `stack`

## Symbols

# Symbols

**Files documented**: 1

## `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Calculation\Calculation.php`

**Classes**:
- `PhpOffice\PhpSpreadsheet\Calculation\Calculation`

**Functions/Methods**:
- `__construct(?Spreadsheet $spreadsheet = null)`
- `loadLocales()`
- `getInstance(?Spreadsheet $spreadsheet = null)`
- `flushInstance()`
- `getDebugLog()`
- `__clone()`
- `getTRUE()`
- `getFALSE()`
- `setArrayReturnType($returnType)`
- `getArrayReturnType()`
- `getCalculationCacheEnabled()`
- `setCalculationCacheEnabled($calculationCacheEnabled)`
- `enableCalculationCache()`
- `disableCalculationCache()`
- `clearCalculationCache()`
- `clearCalculationCacheForWorksheet($worksheetName)`
- `renameCalculationCacheForWorksheet($fromWorksheetName, $toWorksheetName)`
- `setBranchPruningEnabled($enabled)`
- `enableBranchPruning()`
- `disableBranchPruning()`
- `getLocale()`
- `getLocaleFile(string $localeDir, string $locale, string $language, string $file)`
- `setLocale(string $locale)`
- `translateSeparator(string $fromSeparator,
        string $toSeparator,
        string $formula,
        int &$inBracesLevel,
        string $openBrace = self::FORMULA_OPEN_FUNCTION_BRACE,
        string $closeBrace = self::FORMULA_CLOSE_FUNCTION_BRACE)`
- `translateFormulaBlock(array $from,
        array $to,
        string $formula,
        int &$inFunctionBracesLevel,
        int &$inMatrixBracesLevel,
        string $fromSeparator,
        string $toSeparator)`
- `translateFormula(array $from, array $to, string $formula, string $fromSeparator, string $toSeparator)`
- `_translateFormulaToLocale($formula)`
- `_translateFormulaToEnglish($formula)`
- `localeFunc($function)`
- `wrapResult($value)`
- `unwrapResult($value)`
- `calculate(?Cell $cell = null)`
- `calculateCellValue(?Cell $cell = null, $resetLog = true)`
- `parseFormula($formula)`
- `calculateFormula($formula, $cellID = null, ?Cell $cell = null)`
- `getValueFromCache(string $cellReference, &$cellValue)`
- `saveValueToCache($cellReference, $cellValue)`
- `_calculateFormulaValue($formula, $cellID = null, ?Cell $cell = null)`
- `checkMatrixOperands(&$operand1, &$operand2, $resize = 1)`
- `getMatrixDimensions(array &$matrix)`
- `resizeMatricesShrink(&$matrix1, &$matrix2, $matrix1Rows, $matrix1Columns, $matrix2Rows, $matrix2Columns)`
- `resizeMatricesExtend(&$matrix1, &$matrix2, $matrix1Rows, $matrix1Columns, $matrix2Rows, $matrix2Columns)`
- `showValue($value)`
- `showTypeDetails($value)`
- `convertMatrixReferences($formula)`
- `if(strpos($formula, self::FORMULA_OPEN_MATRIX_BRACE)`
- `internalParseFormula($formula, ?Cell $cell = null)`
- `dataTestReference(&$operandData)`
- `processTokenStack($tokens, $cellID = null, ?Cell $cell = null)`
- `validateBinaryOperand(&$operand, &$stack)`
- `executeArrayComparison($operand1, $operand2, $operation, Stack &$stack, bool $recursingArrays)`
- `executeBinaryComparisonOperation($operand1, $operand2, $operation, Stack &$stack, $recursingArrays = false)`
- `executeNumericBinaryOperation($operand1, $operand2, $operation, $matrixFunction, &$stack)`
- `raiseFormulaError(string $errorMessage)`
- `extractCellRange(&$range = 'A1', ?Worksheet $worksheet = null, $resetLog = true)`
- `extractNamedRange(string &$range = 'A1', ?Worksheet $worksheet = null, $resetLog = true)`
- `isImplemented($function)`
- `getFunctions()`
- `getImplementedFunctionNames()`
- `addDefaultArgumentValues(array $functionCall, array $args, array $emptyArguments)`
- `getArgumentDefaultValue(ReflectionParameter $methodArgument)`
- `addCellReference(array $args, $passCellReference, $functionCall, ?Cell $cell = null)`
- `getTokensAsString($tokens)`
- `evaluateDefinedName(Cell $cell, DefinedName $namedRange, Worksheet $cellWorksheet, Stack $stack)`

