# app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Comment.php

- Path: `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Comment.php`
- Type: PHP
- Size: 7531 bytes

## Summary (from docblocks)

Author.
@var string

Rich text comment.
@var RichText

Comment width (CSS style, i.e. XXpx or YYpt).
@var string

Left margin (CSS style, i.e. XXpx or YYpt).
@var string

Top margin (CSS style, i.e. XXpx or YYpt).
@var string

Visible.
@var bool

Comment height (CSS style, i.e. XXpx or YYpt).
@var string

Comment fill color.
@var Color

Alignment.
@var string

Background image in comment.
@var Drawing

Create a new Comment.

Get Author.

Set Author.

Get Rich text comment.

Set Rich text comment.

Get comment width (CSS style, i.e. XXpx or YYpt).

Set comment width (CSS style, i.e. XXpx or YYpt). Default unit is pt.

Get comment height (CSS style, i.e. XXpx or YYpt).

Set comment height (CSS style, i.e. XXpx or YYpt). Default unit is pt.

Get left margin (CSS style, i.e. XXpx or YYpt).

Set left margin (CSS style, i.e. XXpx or YYpt). Default unit is pt.

Get top margin (CSS style, i.e. XXpx or YYpt).

Set top margin (CSS style, i.e. XXpx or YYpt). Default unit is pt.

Is the comment visible by default?

Set comment default visibility.

Set fill color.

Get fill color.

Set Alignment.

Get Alignment.

Get hash code.

Implement PHP __clone to create a deep clone, not just a shallow copy.

Convert to string.

Check is background image exists.

Returns background image.

Sets background image.

Sets size of comment as size of background image.

## Symbols

# Symbols

**Files documented**: 1

## `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Comment.php`

**Classes**:
- `PhpOffice\PhpSpreadsheet\Comment implements IComparable`

**Functions/Methods**:
- `__construct()`
- `getAuthor()`
- `setAuthor(string $author)`
- `getText()`
- `setText(RichText $text)`
- `getWidth()`
- `setWidth(string $width)`
- `getHeight()`
- `setHeight(string $height)`
- `getMarginLeft()`
- `setMarginLeft(string $margin)`
- `getMarginTop()`
- `setMarginTop(string $margin)`
- `getVisible()`
- `setVisible(bool $visibility)`
- `setFillColor(Color $color)`
- `getFillColor()`
- `setAlignment(string $alignment)`
- `getAlignment()`
- `getHashCode()`
- `__clone()`
- `__toString()`
- `hasBackgroundImage()`
- `getBackgroundImage()`
- `setBackgroundImage(Drawing $objDrawing)`
- `setSizeAsBackgroundImage()`

