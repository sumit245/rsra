# app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Chart\Layout.php

- Path: `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Chart\Layout.php`
- Type: PHP
- Size: 8418 bytes

## Summary (from docblocks)

layoutTarget.
@var string

X Mode.
@var string

Y Mode.
@var string

X-Position.
@var float

Y-Position.
@var float

width.
@var float

height.
@var float

show legend key
Specifies that legend keys should be shown in data labels.
@var bool

show value
Specifies that the value should be shown in a data label.
@var bool

show category name
Specifies that the category name should be shown in the data label.
@var bool

show data series name
Specifies that the series name should be shown in the data label.
@var bool

show percentage
Specifies that the percentage should be shown in the data label.
@var bool

show bubble size.
@var bool

show leader lines
Specifies that leader lines should be shown for the data label.
@var bool

Create a new Layout.

Get Layout Target.
@return string

Set Layout Target.
@param string $target
@return $this

Get X-Mode.
@return string

Set X-Mode.
@param string $mode
@return $this

Get Y-Mode.
@return string

Set Y-Mode.
@param string $mode
@return $this

Get X-Position.
@return number

Set X-Position.
@param float $position
@return $this

Get Y-Position.
@return number

Set Y-Position.
@param float $position
@return $this

Get Width.
@return number

Set Width.
@param float $width
@return $this

Get Height.
@return number

Set Height.
@param float $height
@return $this

Get show legend key.
@return bool

Set show legend key
Specifies that legend keys should be shown in data labels.
@param bool $showLegendKey Show legend key
@return $this

Get show value.
@return bool

Set show val
Specifies that the value should be shown in data labels.
@param bool $showDataLabelValues Show val
@return $this

Get show category name.
@return bool

Set show cat name
Specifies that the category name should be shown in data labels.
@param bool $showCategoryName Show cat name
@return $this

Get show data series name.
@return bool

Set show ser name
Specifies that the series name should be shown in data labels.
@param bool $showSeriesName Show series name
@return $this

Get show percentage.
@return bool

Set show percentage
Specifies that the percentage should be shown in data labels.
@param bool $showPercentage Show percentage
@return $this

Get show bubble size.
@return bool

Set show bubble size
Specifies that the bubble size should be shown in data labels.
@param bool $showBubbleSize Show bubble size
@return $this

Get show leader lines.
@return bool

Set show leader lines
Specifies that leader lines should be shown in data labels.
@param bool $showLeaderLines Show leader lines
@return $this

## Symbols

# Symbols

**Files documented**: 1

## `app\ThirdParty\PHPOffice-PhpSpreadsheet\vendor\phpoffice\phpspreadsheet\src\PhpSpreadsheet\Chart\Layout.php`

**Classes**:
- `PhpOffice\PhpSpreadsheet\Chart\Layout`

**Functions/Methods**:
- `__construct(array $layout = [])`
- `getLayoutTarget()`
- `setLayoutTarget($target)`
- `getXMode()`
- `setXMode($mode)`
- `getYMode()`
- `setYMode($mode)`
- `getXPosition()`
- `setXPosition($position)`
- `getYPosition()`
- `setYPosition($position)`
- `getWidth()`
- `setWidth($width)`
- `getHeight()`
- `setHeight($height)`
- `getShowLegendKey()`
- `setShowLegendKey($showLegendKey)`
- `getShowVal()`
- `setShowVal($showDataLabelValues)`
- `getShowCatName()`
- `setShowCatName($showCategoryName)`
- `getShowSerName()`
- `setShowSerName($showSeriesName)`
- `getShowPercent()`
- `setShowPercent($showPercentage)`
- `getShowBubbleSize()`
- `setShowBubbleSize($showBubbleSize)`
- `getShowLeaderLines()`
- `setShowLeaderLines($showLeaderLines)`

