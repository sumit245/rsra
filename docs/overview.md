# Project Overview

**Framework**: CodeIgniter 4.x (PHP)
**App path**: `app/`
**Public entry**: `index.php`
**Views**: `app/Views`
**Assets**: `assets/`
**Plugins/Modules**: `plugins/`

This document set was generated automatically to enumerate the complete file list and summarize classes and functions across controllers, models, and the system core. The UI layer under `app/Views` is composed of PHP view templates grouped by feature.
